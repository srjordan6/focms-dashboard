/**
 * FOCMS Dashboard - JavaScript
 *
 * Phase 3.1: placeholder. No interactive widgets yet.
 * Phase 3.2+: chart rendering, tab transitions, form handlers, live updates.
 *
 * Loaded only on dashboard routes (see focms_dashboard_enqueue_assets in main plugin).
 */

(function ($) {
    'use strict';

    $(document).ready(function () {
        // Phase 3.1 has nothing to bind yet.
        // Structure is here so Phase 3.2 widgets just register here.
        console.log('FOCMS Dashboard loaded.');
    });

})(jQuery);
